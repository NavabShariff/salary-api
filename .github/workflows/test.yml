name: test

on:
 push:
  branches: ['main']

jobs:
 test:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v4
    with:
      sparse-checkout: |
        ecr-image-scan.sh
      sparse-checkout-cone-mode: false

  - name: test
    run: |
      ls -lh